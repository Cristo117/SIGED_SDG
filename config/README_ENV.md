# Configuración con variables de entorno

## Instalación

1. Copie  `.env`:
   ```
   
   ```

2. Edite `.env` con sus credenciales de base de datos:
   ```
   DB_HOST=localhost
   DB_NAME=siged_sdg
   DB_USER=root
   DB_PASS=su_contraseña
   APP_ENV=development
   ```
   En **producción**, use `APP_ENV=production` para ocultar errores PHP al usuario.


